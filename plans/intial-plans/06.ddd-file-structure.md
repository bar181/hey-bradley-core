# HEY BRADLEY — Document 6: File Structure, DDD, & AISP Specs

**Version:** 3.0.0 | **Date:** March 27, 2026 | **Author:** Bradley Ross  
**Status:** FINAL — Build-Ready | **Target Audience:** AI Agents (Claude Code, Cursor, Devin), Senior Agentic Engineers

---

## 1. COMPLETE FILE STRUCTURE

```
hey-bradley/
├── public/
│   └── favicon.svg
├── src/
│   ├── App.tsx                              # Router + AppShell wrapper
│   ├── main.tsx                             # Entry point
│   │
│   ├── styles/
│   │   ├── tokens.css                       # CSS custom properties (Doc 4 §2.1)
│   │   └── fonts.css                        # Google Fonts: DM Sans + JetBrains Mono
│   │
│   ├── store/
│   │   ├── configStore.ts                   # Master JSON config (Zustand)
│   │   ├── uiStore.ts                       # UI mode state (LISTEN/BUILD, DRAFT/EXPERT)
│   │   ├── chatStore.ts                     # Chat history + pending patches (Phase 5+)
│   │   └── undoMiddleware.ts                # Undo/redo middleware for configStore
│   │
│   ├── lib/
│   │   ├── cn.ts                            # clsx + tailwind-merge utility
│   │   ├── deepMerge.ts                     # JSON merge (objects deep, arrays replace)
│   │   ├── validation.ts                    # Shared Zod validators
│   │   └── schemas/
│   │       ├── masterConfig.ts              # Root schema: spec, page, version, sections[]
│   │       ├── section.ts                   # Base section schema (type, id, layout, content, style)
│   │       ├── layout.ts                    # Layout schema (display, direction, align, gap, padding)
│   │       ├── style.ts                     # Style schema (background, color, fontFamily, borderRadius)
│   │       ├── patch.ts                     # LLM patch envelope schema
│   │       └── events.ts                    # Domain event type schemas
│   │
│   ├── types/
│   │   ├── config.ts                        # z.infer exports for all schemas
│   │   ├── modes.ts                         # InteractionMode, ComplexityMode, UIState
│   │   └── sections.ts                      # SectionType union, per-section content types
│   │
│   ├── components/
│   │   ├── ui/                              # shadcn/ui components (Button, Input, etc.)
│   │   │
│   │   ├── shell/                           # App-level layout
│   │   │   ├── AppShell.tsx                 # Top bar + layout wrapper
│   │   │   ├── TopBar.tsx                   # Logo, project name, version badge, device toggles
│   │   │   ├── ModeToggle.tsx               # LISTEN/BUILD + DRAFT/EXPERT pill toggles
│   │   │   ├── PanelLayout.tsx              # Three resizable panels
│   │   │   ├── StatusBar.tsx                # Bottom: READY AISP SPEC V1.2 | MODE: DRAFT CONNECTED
│   │   │   └── ChatInput.tsx                # Mic + text input + send (below left panel)
│   │   │
│   │   ├── left-panel/                      # Left panel variants by mode
│   │   │   ├── DraftPanel.tsx               # "Draft Builder" — vibe cards, section list
│   │   │   ├── ExpertPanel.tsx              # "Expert Editor" — accordion property inspector
│   │   │   ├── VibeCards.tsx                # 3 vibe preset cards (Warm, Ocean, Forest)
│   │   │   ├── SectionList.tsx              # Numbered section cards with arrows
│   │   │   ├── PropertyInspector.tsx        # Expert: key-value property rows
│   │   │   ├── ProjectExplorer.tsx          # Expert: tree view (HeroSection > Title, etc.)
│   │   │   └── AccordionSection.tsx         # Reusable: LAYOUT, CONTENT, STYLE, IMAGES accordion
│   │   │
│   │   ├── center-canvas/                   # Four center tabs
│   │   │   ├── TabBar.tsx                   # REALITY | DATA | XAI DOCS | WORKFLOW
│   │   │   ├── RealityTab.tsx               # Live preview with section selection
│   │   │   ├── DataTab.tsx                  # JSON viewer/editor with syntax highlighting
│   │   │   ├── XAIDocsTab.tsx               # HUMAN/AISP sub-toggle + spec content
│   │   │   ├── WorkflowTab.tsx              # Pipeline stepper + live log
│   │   │   └── SectionWrapper.tsx           # Click-to-select border, hover state
│   │   │
│   │   ├── right-panel/                     # Right panel variants by mode
│   │   │   ├── DraftContext.tsx             # Simplified: Headline, Layout cards, "More options →"
│   │   │   └── ExpertContext.tsx            # Full: Layout, Content, Style with accordions
│   │   │
│   │   └── listen-mode/                     # Listen mode overlay
│   │       ├── ListenOverlay.tsx            # Dark bg transition + layout
│   │       ├── RedOrb.tsx                   # Layered CSS: core + glow + ambient + pulse
│   │       ├── Typewriter.tsx               # Character-by-character text render
│   │       └── StartListeningButton.tsx     # Pill button below orb
│   │
│   ├── templates/                           # Section renderer components (pure UI)
│   │   ├── registry.ts                      # SectionType → variant → component map
│   │   ├── hero/
│   │   │   ├── HeroCentered.tsx
│   │   │   ├── HeroSplit.tsx
│   │   │   ├── HeroOverlay.tsx
│   │   │   ├── HeroFullImage.tsx
│   │   │   ├── HeroMinimal.tsx
│   │   │   └── schema.ts                   # Zod schema for hero section
│   │   ├── features/
│   │   │   ├── FeaturesGrid3.tsx
│   │   │   ├── FeaturesGrid4.tsx
│   │   │   ├── FeaturesAlternating.tsx
│   │   │   ├── FeaturesCards.tsx
│   │   │   └── schema.ts
│   │   ├── pricing/
│   │   │   ├── Pricing2Tier.tsx
│   │   │   ├── Pricing3Tier.tsx
│   │   │   ├── PricingComparison.tsx
│   │   │   └── schema.ts
│   │   ├── cta/
│   │   │   ├── CTASimple.tsx
│   │   │   ├── CTASplit.tsx
│   │   │   ├── CTAWithImage.tsx
│   │   │   ├── CTAGradient.tsx
│   │   │   └── schema.ts
│   │   ├── footer/
│   │   │   ├── FooterSimple.tsx
│   │   │   ├── FooterMultiColumn.tsx
│   │   │   ├── FooterMinimal.tsx
│   │   │   └── schema.ts
│   │   ├── testimonials/
│   │   │   ├── TestimonialCards.tsx
│   │   │   ├── TestimonialCarousel.tsx
│   │   │   ├── TestimonialQuote.tsx
│   │   │   └── schema.ts
│   │   ├── faq/
│   │   │   ├── FAQAccordion.tsx
│   │   │   ├── FAQTwoColumn.tsx
│   │   │   └── schema.ts
│   │   └── value-props/
│   │       ├── ValuePropsIcons.tsx
│   │       ├── ValuePropsNumbers.tsx
│   │       ├── ValuePropsCards.tsx
│   │       └── schema.ts
│   │
│   ├── contexts/                            # DDD bounded contexts (non-UI logic)
│   │   ├── intelligence/                    # LLM + voice (Phase 5+)
│   │   │   ├── services/
│   │   │   │   ├── intentClassifier.ts
│   │   │   │   ├── patchGenerator.ts
│   │   │   │   ├── copyAdvisor.ts
│   │   │   │   └── voiceProcessor.ts
│   │   │   └── prompts/
│   │   │       ├── classifyIntent.ts
│   │   │       ├── generatePatch.ts
│   │   │       ├── suggestCopy.ts
│   │   │       └── aisp/
│   │   │           └── crystalAtom.ts
│   │   │
│   │   ├── specification/                   # Spec generation
│   │   │   ├── services/
│   │   │   │   ├── specGenerator.ts
│   │   │   │   ├── sectionSpecGen.ts
│   │   │   │   └── aispFormatter.ts
│   │   │   └── templates/
│   │   │       ├── northStar.ts
│   │   │       ├── architecture.ts
│   │   │       └── implPlan.ts
│   │   │
│   │   └── persistence/                     # Storage abstraction
│   │       ├── interfaces/
│   │       │   └── IProjectRepository.ts
│   │       └── adapters/
│   │           ├── localStorageAdapter.ts
│   │           └── supabaseAdapter.ts       # Phase 4
│   │
│   ├── pages/                               # Route-level components
│   │   ├── Onboarding.tsx                   # Vibe selection page (Phase 2.1)
│   │   ├── Builder.tsx                      # Main builder (PanelLayout + tabs)
│   │   ├── Login.tsx                        # Phase 4
│   │   └── Dashboard.tsx                    # Phase 4
│   │
│   └── presets/                             # Hardcoded defaults (→ DB in Phase 4.2)
│       ├── vibes/
│       │   ├── warm.json
│       │   ├── ocean.json
│       │   └── forest.json
│       └── sections/
│           ├── hero-defaults.json
│           ├── features-defaults.json
│           ├── pricing-defaults.json
│           ├── cta-defaults.json
│           ├── footer-defaults.json
│           ├── testimonials-defaults.json
│           ├── faq-defaults.json
│           └── value-props-defaults.json
│
├── index.html
├── package.json
├── tsconfig.json
├── tailwind.config.ts                       # Extended with Hey Bradley tokens
├── vite.config.ts                           # Path aliases
├── postcss.config.js
└── .env                                     # Supabase keys (Phase 4)
```

---

## 2. DDD DOMAIN EVENTS

```aisp
⟦
  Ω := { Define Hey Bradley domain event catalog }
  Σ := { Event:{name:𝕊, publisher:Context, subscribers:[Context], payload:Record}, Context:{"builder"|"intelligence"|"specification"|"persistence"} }
  Γ := { R1: ∀ e∈Event : e.publisher publishes via Zustand, R2: ∀ subscriber : reads via store.subscribe(), R3: ¬∃ direct_import(builder, intelligence) }
  Λ := { event_bus:="zustand_subscriptions" }
  Ε := { V1: VERIFY no cross-context imports in import graph }
⟧
```

| Event | Publisher | Subscribers | Payload |
|-------|----------|-------------|---------|
| `config.changed` | configStore | RealityTab, DataTab, XAIDocsTab, AutoSave | `{ patch, source }` |
| `config.theme_changed` | configStore | All section renderers (cascade) | `{ theme }` |
| `config.section_added` | configStore | SectionList, RealityTab | `{ section }` |
| `config.section_removed` | configStore | SectionList, RealityTab | `{ sectionId }` |
| `config.section_reordered` | configStore | SectionList, RealityTab | `{ newOrder }` |
| `ui.mode_changed` | uiStore | LeftPanel, RightPanel, ListenOverlay | `{ interaction, complexity }` |
| `ui.tab_changed` | uiStore | TabBar, center canvas | `{ activeTab }` |
| `ui.section_selected` | uiStore | RightPanel, SectionWrapper | `{ sectionId }` |
| `llm.patch_received` | chatStore | configStore (via merge) | `{ patch, confidence }` |
| `llm.processing` | chatStore | WorkflowTab (pipeline update) | `{ step, status }` |
| `voice.transcript` | voiceProcessor | ChatInput (show text) | `{ text, isFinal }` |

---

## 3. CONFIGSTORE SPECIFICATION

```aisp
⟦
  Ω := { Define Zustand configStore contract }
  Σ := { ConfigStore:{config:MasterConfig, history:[MasterConfig], future:[MasterConfig], isDirty:𝔹, lastSavedAt:Date|null}, PatchSource:{"ui"|"json-editor"|"llm-chat"|"llm-voice"|"llm-listen"|"import"|"preset"|"vibe"} }
  Γ := { R1: ∀ mutation : push_prev_to_history_first, R2: ∀ applyPatch : arrays_replace_entirely, R3: ∀ applyPatch(null_value) : delete_key, R4: history.length ≤ 100, R5: ¬∃ useState_for_config_data }
  Λ := { history_depth:=100, autosave_debounce:=2000 }
  Ε := { V1: VERIFY undo restores previous state exactly, V2: VERIFY array merge replaces (never concats), V3: VERIFY all config mutations go through named methods }
⟧
```

**Store Interface:**

```typescript
interface ConfigStore {
  // STATE
  config: MasterConfig;
  history: MasterConfig[];
  future: MasterConfig[];
  isDirty: boolean;
  lastSavedAt: Date | null;

  // MUTATIONS (all changes go through these)
  applyPatch: (patch: DeepPartial<MasterConfig>, source: PatchSource) => void;
  applyVibe: (vibeName: string) => void;  // Macro: changes 15+ nodes
  addSection: (type: SectionType, afterIndex?: number) => void;
  removeSection: (sectionId: string) => void;
  reorderSections: (newOrder: string[]) => void;
  setSectionConfig: (sectionId: string, patch: DeepPartial<SectionConfig>) => void;

  // HISTORY
  undo: () => void;
  redo: () => void;

  // PERSISTENCE
  markSaved: () => void;
  loadConfig: (config: MasterConfig) => void;
  resetToDefaults: () => void;
}
```

---

## 4. DEEP MERGE SPECIFICATION

```aisp
⟦
  Ω := { Define JSON deep merge rules for LLM patch application }
  Σ := { MergeRule:{"deep_object"|"replace_array"|"delete_null"|"skip_undefined"|"overwrite_primitive"} }
  Γ := { R1: IF typeof(patch)="object" ∧ ¬Array.isArray(patch) THEN deep_merge_recursive, R2: IF Array.isArray(patch) THEN REPLACE_entirely, R3: IF patch_value=null THEN DELETE_key, R4: IF patch_value=undefined THEN SKIP, R5: IF primitive THEN OVERWRITE }
  Λ := { implementation:="src/lib/deepMerge.ts" }
  Ε := { V1: VERIFY merge({a:{b:1}}, {a:{c:2}}) = {a:{b:1,c:2}}, V2: VERIFY merge({items:[1,2]}, {items:[3]}) = {items:[3]}, V3: VERIFY merge({a:1}, {a:null}) = {} }
⟧
```

---

## 5. AISP SPEC EXAMPLES (For XAI Docs Tab Output)

### 5.1 How the XAI Docs Tab Renders AISP

When the user switches to the AISP sub-toggle in the XAI DOCS tab, the system generates AISP-formatted specs from the current JSON config. This is the format that appears (matching mockup Image 5):

```
@aisp 1.2
@page index
@version 1.0.0-RC1

@section hero hero-01 {
    @layout flex column center
    @gap 24px
    @padding 64px

    @heading[1] "Build with voice." {
        @size 48px
        @weight 700
    }

    @subheading "Ship in minutes."

    @cta "Get Started" → #pricing
}

@section features features-01 {
    @layout grid 3col
    @gap 32px
    @padding 48px

    @item { @icon "⚡" @title "Fast" }
    @item { @icon "🎯" @title "Precise" }
    @item { @icon "🔒" @title "Secure" }
}

@section cta cta-01 {
    @layout flex center
    @padding 32px
    @bg #e8772e

    @heading "Ready to start?"
    @button "Launch Now" → #signup
}
```

### 5.2 AISP Crystal Atom for the MasterConfig Schema

```aisp
⟦
  Ω := { Validate Hey Bradley site configuration }
  Σ := { Site:{spec:"aisp-1.2", page:𝕊, version:𝕊, sections:[Section]}, Section:{type:SectionType, id:𝕊, layout:Layout, content:Content, style:Style}, SectionType:{"hero"|"features"|"pricing"|"cta"|"footer"|"testimonials"|"faq"|"value_props"}, Layout:{display:{"flex"|"grid"}, direction?:{"row"|"column"}, align?:{"center"|"start"|"end"}, gap:𝕊, padding:𝕊} }
  Γ := { R1: ∀ s∈sections : unique(s.id), R2: ∀ s : s.type∈SectionType, R3: ∀ items_array : ∀ item : exists(item.id), R4: |sections| ≥ 1 }
  Λ := { spec_version:="aisp-1.2", max_sections:=20 }
  Ε := { V1: VERIFY JSON.parse(export)∈Site, V2: VERIFY Zod.parse(config).success=true, V3: VERIFY Ambig(spec)<0.02 }
⟧
```

### 5.3 AISP for the deepMerge Function

```aisp
⟦
  Ω := { Deep merge JSON patch into target config }
  Σ := { input:⟨target:JSON, patch:JSON⟩, output:JSON }
  Γ := { R1: IF isObject(v)∧¬isArray(v) THEN recurse, R2: IF isArray(v) THEN replace, R3: IF v=null THEN delete, R4: IF v=undefined THEN skip, R5: IF isPrimitive(v) THEN overwrite }
  Λ := { location:="src/lib/deepMerge.ts" }
  Ε := { V1: VERIFY merge({a:{b:1}},{a:{c:2}})={a:{b:1,c:2}}, V2: VERIFY merge({x:[1,2]},{x:[3]})={x:[3]}, V3: VERIFY merge({a:1},{a:null})={} }
⟧
```

### 5.4 AISP for the Intent Classifier (Phase 5)

```aisp
⟦
  Ω := { Classify user text into target section and change type }
  Σ := { input:𝕊, output:⟨targetSection:SectionType|"theme", changeType:{"content"|"layout"|"style"|"structure"|"theme"}, confidence:ℝ[0,1]⟩ }
  Γ := { R1: IF confidence<0.7 THEN ask_clarification, R2: IF keywords∈{"dark","light","color","font","vibe"} THEN targetSection="theme", R3: IF keywords∈{"headline","title","text","copy"} THEN changeType="content", R4: IF keywords∈{"add","remove","reorder"} THEN changeType="structure" }
  Λ := { model:="claude-sonnet-4-20250514", max_tokens:=200, temperature:=0.1 }
  Ε := { V1: VERIFY "change the headline"→{targetSection:"hero",changeType:"content"}, V2: VERIFY "make it dark"→{targetSection:"theme",changeType:"theme"}, V3: VERIFY response_time<500ms }
⟧
```

---

## 6. WORKFLOW PIPELINE SPECIFICATION

The WORKFLOW tab shows the LLM processing pipeline. Each step has three states:

```aisp
⟦
  Ω := { Define LLM update pipeline for Workflow tab }
  Σ := { PipelineStep:{name:𝕊, status:{"completed"|"active"|"waiting"|"error"}, subtitle:𝕊}, Pipeline:[PipelineStep] }
  Γ := { R1: steps execute sequentially, R2: IF step.status="error" THEN pipeline.halt, R3: ∀ completed_step : show green_check, R4: active_step : show orange_spinner, R5: waiting_step : show gray_clock }
  Λ := { steps:=["Voice Capture","Intent Parsing","AISP Generation","Schema Validation","Reality Render","Edge Deploy"] }
  Ε := { V1: VERIFY pipeline renders all 6 steps, V2: VERIFY active step shows spinner animation, V3: VERIFY live log updates with timestamps }
⟧
```

**Pipeline Steps:**

| Step | Name | Active Subtitle | Completed Subtitle |
|------|------|----------------|-------------------|
| 1 | Voice Capture | "Listening..." | "Captured 3 intents" |
| 2 | Intent Parsing | "Analyzing..." | "hero, features, cta" |
| 3 | AISP Generation | "Generating..." | "3 sections generated" |
| 4 | Schema Validation | "Validating aisp-1.2..." | "All sections valid" |
| 5 | Reality Render | "Rendering..." | "Preview updated" |
| 6 | Edge Deploy | "Deploying..." | "Live at preview URL" |

**Live Stream Output (below pipeline):**
```
[00:01] Voice → "hero section, dark background"
[00:02] Intent parsed: type=hero, bg=#0a0a0f
[00:03] AISP generated: @section hero hero-01
[00:04] Validating schema... ▌
```

---

## 7. ROUTING

```
/                    → Onboarding (unauthenticated) or Dashboard (Phase 4)
/builder             → Builder page (default project or new)
/builder/:projectId  → Builder for specific project (Phase 4)
/login               → Auth page (Phase 4)
/dashboard           → Project list (Phase 4)
```

For Phases 1-3 (no auth): app loads directly into `/builder` with a default project from localStorage.

---

## 8. TAILWIND CONFIGURATION

```typescript
// tailwind.config.ts
export default {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        hb: {
          bg: '#faf8f5',
          surface: '#f5f0eb',
          'surface-hover': '#efe8e0',
          border: '#e8e0d8',
          'border-selected': '#e8772e',
          'text-primary': '#2d1f12',
          'text-secondary': '#8a7a6d',
          'text-muted': '#b5a99d',
          accent: '#e8772e',
          'accent-light': '#fce8d8',
          'accent-hover': '#d46820',
          success: '#4a9d6e',
          warning: '#d4a12e',
          error: '#c44a4a',
          'listen-bg': '#0a0a0f',
          'listen-orb': '#ef4444',
        },
      },
      fontFamily: {
        ui: ['DM Sans', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'Fira Code', 'SF Mono', 'monospace'],
      },
      animation: {
        'orb-pulse': 'orbPulse 2s ease-in-out infinite',
        'orb-breathe': 'orbBreathe 4s ease-in-out infinite',
        'orb-active': 'orbActive 1.2s ease-in-out infinite',
      },
      keyframes: {
        orbPulse: { '0%, 100%': { transform: 'scale(1)' }, '50%': { transform: 'scale(1.1)' } },
        orbBreathe: { '0%, 100%': { opacity: '0.1' }, '50%': { opacity: '0.2' } },
        orbActive: { '0%, 100%': { transform: 'scale(1)' }, '50%': { transform: 'scale(1.3)' } },
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
}
```

---

## 9. COMPLETE DOCUMENT INDEX

| Doc | Title | Purpose | Key Contents |
|-----|-------|---------|-------------|
| **1** | North Star | Vision, positioning, success criteria | PMF equation, user personas, coverage analysis, "aha!" moments |
| **2** | Architecture & Data Model | Technical architecture, JSON schema, ADRs | Two-mode architecture, four tabs, data model, 11 ADRs, DDD contexts, import firewall |
| **3** | Implementation Plan | Phase-by-phase build instructions | 21 phases across 7 levels, deliverables, acceptance criteria, dependency graph |
| **4** | Design Bible | Visual design specification | Color system, typography, component specs, motion, responsive, best-in-class references |
| **5** | Swarm Protocol | Agent behavioral rules | Execution sequence, state laws, styling firewall, dependency whitelist, cardinal sins |
| **6** | File Structure, DDD & AISP | Technical implementation details | Directory tree, domain events, configStore contract, deepMerge spec, AISP examples, routing, Tailwind config |

**Reading Order for Agents:**
1. Read Doc 1 (vision context)
2. Read Doc 5 (behavioral rules — the "law")
3. Read Doc 4 (visual targets — the "look")
4. Read Doc 2 (architecture — the "how")
5. Read Doc 6 (file structure — the "where")
6. Read Doc 3 (implementation plan — the "when")
7. Build Phase 1.0

---

*End of Document 6*
*This completes the Hey Bradley specification package. All 6 documents are consistent and build-ready.*