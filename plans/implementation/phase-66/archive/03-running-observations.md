# P66 / Polish Sprint — Running Observations

> **Purpose:** Track additional polish gaps + improvement opportunities as Wave-1 sub-modules complete. Items added here become carry-forward candidates for OC-CLEANUP / OC-4 / future polish sprints.
> **Updated:** appended after each agent return; A7 consumes for the post-review doc.

---

## A0 baseline (from `00-before-state.md`)

Library average: **~6.0/10**. 22 polish items ranked P1/P2/P3. Targeted closure: 17/22 (77%).

---

## Pre-Wave-1 staging notes (orchestrator)

- Pre-created `src/demos/{ListenModeDemo,ChatModeDemo}.tsx` as stubs so the `/demo/listen` + `/demo/chat` routes resolve immediately (build won't break if an agent times out)
- Added both routes in `src/main.tsx` (success path + persistence-error path) — A1/A2 own ONLY their respective demo file replacement; do NOT touch main.tsx
- This pre-stage commits at the same time as A0 so Wave 1 can dispatch cleanly

---

## Per-agent observations (filled as agents return)

### A1 — Listen Mode Demo
- TBD on agent return

### A2 — Chat Mode Demo
- TBD on agent return

### A3 — Mobile Sub-Module
- TBD on agent return

### A4 — Onboarding Sub-Module
- TBD on agent return

### A5 — Template + Builder Sub-Module
- TBD on agent return

### A6 — LLM Persona Sub-Module
- TBD on agent return

---

## Discovered carry-forward (additions to backlog)

- TBD as observations land

---

## Final notes (filled at A7 seal)

- TBD
