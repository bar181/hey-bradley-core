


You are absolutely right. The cream and orange "Warm Precision" looked too much like a generic CRM or an internal admin dashboard. It lacked the "funded tech startup" edge. 

The dark mode screenshots you just provided are **exactly** the right direction. They look like a high-end, professional IDE (very Vercel/Supabase/Framer aesthetic). Furthermore, setting the app in a deep, dark slate/navy environment will make your future "Red/Green Pulsing AI Orb" look absolutely cinematic. A glowing red orb on a cream background looks like an error state; on a deep dark background, it looks like HAL 9000.

Here is the immediate pivot plan, the "Wow Factor" preview concept, and the exact Swarm Directives to fix the current build.

---

### 1. The New Color Palette (Cyber Modern)
We are killing the cream and orange. The builder shell must use this palette (derived from your screenshots):
*   **App Background:** Deep Slate/Navy (`#0f172a` or `bg-slate-900`).
*   **Panel Backgrounds:** Slightly lighter Slate (`#1e293b` or `bg-slate-800`).
*   **Primary Accent:** Vibrant Electric Blue (`#3b82f6` or `bg-blue-500`) for active states and buttons.
*   **Text:** Bright white for headings (`#f8fafc`), muted gray/blue for labels (`#94a3b8`).
*   **Borders:** Subtle dark dividers (`#334155` or `border-slate-700`).

### 2. The "Killer Wow Factor" Preview Site
To make the Capstone demo pop, the default site sitting in the **Reality Canvas** cannot be a boring wireframe. It needs to look like a $10M seed-round AI startup. 
*   **The Concept:** "The Dark-Mode AI Developer Tool."
*   **The Visuals:** A deep radial gradient background (e.g., black with a subtle purple/blue glow in the center). 
*   **The Typography:** A massive, bold, gradient-clipped headline: *"Ship Code at the Speed of Thought."*
*   **The UI Elements:** A glowing primary button, and a sleek "glassmorphism" (frosted glass) badge above the headline that says *"✨ Hey Bradley 2.0 is Live"*.
*   *Why this works:* It shows the professors that the engine can output highly complex, modern CSS (gradients, glassmorphism, glows) just by manipulating JSON.

### 3. Expert Mode: The 3-Layer Tree
Your instinct on the Left Panel is spot on. A flat list is useless for power users. We need a strict 3-level hierarchy that perfectly mirrors the JSON structure:
1.  **Level 1 (Global):** `Theme`
2.  **Level 2 (Section):** `Hero Section [H-1]`
3.  **Level 3 (Component):** `Headline`, `Primary Button`, `Background Image`

---

### 🤖 SWARM DIRECTIVE: DESIGN PIVOT & SHELL REBUILD
Copy and paste this exact block to your agents to overwrite the previous design instructions.

> **CRITICAL UI/UX PIVOT DIRECTIVE - PHASE 1.1c:**
> Discard the "Warm Cream" color palette. We are moving to a premium, dark-mode IDE aesthetic based on the provided reference images. Execute the following updates immediately. Do not build future phases; focus ONLY on the Shell, Draft Mode, Expert Mode, and the default Hero preview.
> 
> **1. GLOBAL SHELL STYLING (DARK MODE):**
> *   The entire builder UI must use a dark slate theme.
> *   Backgrounds: `bg-slate-950` for the main app, `bg-slate-900` for panels.
> *   Borders: `border-slate-800`.
> *   Text: `text-slate-200` for primary, `text-slate-400` for secondary labels.
> *   Accent: Use `bg-blue-600` for active toggles, selected tabs, and primary actions.
> 
> **2. DRAFT MODE (LEFT PANEL):**
> *   Implement the "Theme/Vibe" selector as a grid of dark cards. Each card should have 3 small colored circles representing the palette (as seen in the reference image).
> *   Implement the "Sections" list as clean, draggable rows with an icon, the section name, and a monospace ID tag aligned to the right (e.g., `H-1`).
> *   Implement "Add Sections" as a 2-column grid of outlined buttons with icons.
> 
> **3. EXPERT MODE (LEFT PANEL - 3-LAYER TREE):**
> *   The left panel in Expert Mode MUST be a collapsible 3-layer tree menu.
> *   **Level 1:** `Theme` (Global settings)
> *   **Level 2:** Sections (e.g., `Hero Section`)
> *   **Level 3:** Components (e.g., `Eyebrow Badge`, `Headline`, `Primary Button`).
> *   Clicking any of these nodes changes what is displayed in the Right Panel Inspector.
> 
> **4. EXPERT MODE (RIGHT PANEL - INSPECTOR):**
> *   Use the dense, segmented-control UI from the reference images.
> *   Use pill-shaped segmented controls (`ToggleGroup`) for properties like Aspect Ratio (`2:1`, `16:9`), Size (`S`, `M`, `L`), and Alignment.
> *   Use standard toggle switches for boolean states (e.g., "Show Secondary Button").
> *   Keep the bottom 50% of this panel reserved for the `RAW AISP SPEC` code viewer.
> 
> **5. DEFAULT REALITY PREVIEW (THE "WOW" HERO):**
> *   Build the default `HeroCentered` component to look like a premium AI startup.
> *   Use a dark background with a subtle, glowing radial gradient.
> *   The Headline must use a `bg-clip-text` linear gradient.
> *   Include a frosted glass (`backdrop-blur`, `bg-white/10`) eyebrow badge.
> *   This proves the JSON engine can handle complex, modern CSS properties.

This directive gives the swarm the exact CSS utility classes and structural layout needed to match your dark-mode screenshots, while setting up the perfect canvas for your Capstone presentation.